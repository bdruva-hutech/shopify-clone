{{ 'custom-contact.css' | asset_url | stylesheet_tag }}

{% comment %}
  <div
    class="contact-section slider-{{ section.settings.color_scheme }}"
    style="
      padding-top: {{ section.settings.padding_top }}px;
      padding-bottom: {{ section.settings.padding_bottom }}px;
      padding-left: {{ section.settings.padding_left }}px;
      padding-right: {{ section.settings.padding_right }}px;
    "
  >
    <div class="custom-contact-form">
      {%- if section.settings.color_scheme == 'dark' -%}
        <div class="custom-contact-form__background"></div>
      {%- endif -%}
      {%- if section.settings.color_scheme == 'light' -%}
        <div class="custom-contact-form__background light"></div>
      {%- endif -%}
      {%- if section.settings.heading != blank -%}
        <h2 class="title title-wrapper--no-top-margin inline-richtext {{ section.settings.heading_size }}">
          {{ section.settings.heading }}
        </h2>
      {%- endif -%}
      {%- form 'contact', id: 'ContactForm', class: 'isolate' -%}
        <div class="form-grid">
          <!-- First Name (Shown/Hidden based on toggle) -->
          {% if section.settings.show_first_name %}
            <div class="field">
              <input type="text" id="ContactFormFirstName" name="contact[first_name]" placeholder="First Name" required>
            </div>
          {% endif %}

          <!-- Last Name (Shown/Hidden based on toggle) -->
          {% if section.settings.show_second_name %}
            <div class="field">
              <input type="text" id="ContactFormLastName" name="contact[last_name]" placeholder="Last Name" required>
            </div>
          {% endif %}

          <!-- Email -->
          <div class="field">
            <input type="email" id="ContactFormEmail" name="contact[email]" placeholder="Email *" required>
          </div>

          <!-- Phone full width -->
          <div class="field">
            <input
              type="tel"
              id="ContactFormPhone"
              name="contact[phone]"
              placeholder="Phone Number"
              pattern="[0-9]{10}"
              maxlength="10"
              required
            >
          </div>
        </div>

        <!-- Message full width -->
        <div class="form-grid">
          <textarea
            id="ContactFormMessage"
            name="contact[body]"
            rows="6"
            placeholder="Message"
            style="min-height: 150px; grid-column: 1 / 3;"
          ></textarea>
        </div>

        <!-- Submit -->
        <button type="submit" class="btn">Send</button>
      {%- endform -%}
    </div>
  </div>
{% endcomment %}
<div
  class="contact-section slider-{{ section.settings.color_scheme }}"
  style="
    padding-top: {{ section.settings.padding_top }}px;
    padding-bottom: {{ section.settings.padding_bottom }}px;
    padding-left: {{ section.settings.padding_left | default: 20 }}px;
    padding-right: {{ section.settings.padding_right | default: 20 }}px;
  "
>
  <div class="custom-contact-form">
    {% if section.settings.heading != blank %}
      <h2 class="title title-wrapper--no-top-margin inline-richtext {{ section.settings.heading_size }}">
        {{ section.settings.heading }}
      </h2>
    {% endif %}

    {%- form 'contact', id: 'ContactForm', class: 'isolate' -%}
      <div class="form-grid">
        {% if section.settings.show_first_name %}
          <div class="field">
            <input type="text" id="ContactFormFirstName" name="contact[first_name]" placeholder="First Name" required>
          </div>
        {% endif %}
        {% if section.settings.show_second_name %}
          <div class="field">
            <input type="text" id="ContactFormLastName" name="contact[last_name]" placeholder="Last Name" required>
          </div>
        {% endif %}
        <div class="field">
          <input type="email" id="ContactFormEmail" name="contact[email]" placeholder="Email *" required>
        </div>
        <div class="field">
          <input
            type="tel"
            id="ContactFormPhone"
            name="contact[phone]"
            placeholder="Phone Number"
            pattern="[0-9]{10}"
            maxlength="10"
            required
          >
        </div>
      </div>
      <div class="form-grid">
        <textarea id="ContactFormMessage" name="contact[body]" rows="6" placeholder="Message"></textarea>
      </div>
      <button type="submit" class="btn">Send</button>
    {%- endform -%}
  </div>
</div>
{%- style -%}
  /* Light scheme */
  .slider-light .custom-contact-form,
  .slider-light.contact-section {
    background: #fff;
    color: #333;
  }

  /* Dark scheme */
  .slider-dark .custom-contact-form,
  .slider-dark.contact-section {
    background: #111;
    color: #eee;
  }
{%- endstyle -%}

{% schema %}
{
  "name": "Custom Contact Form",
  "tag": "section",
  "class": "section",
  "settings": [
    {
      "type": "inline_richtext",
      "id": "heading",
      "label": "Heading",
      "default": "Contact form"
    },
    {
      "type": "checkbox",
      "id": "show_first_name",
      "label": "Show First Name",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_second_name",
      "label": "Show Last Name",
      "default": true
    },
    {
      "type": "select",
      "id": "color_scheme",
      "label": "Color Scheme",
      "options": [
        { "value": "light", "label": "Light" },
        { "value": "dark", "label": "Dark" }
      ],
      "default": "light"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Padding top",
      "default": 36
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Padding bottom",
      "default": 36
    }
  ],
  "presets": [
    {
      "name": "Custom contact form"
    }
  ]
}
{% endschema %}
{% comment %}
  {{ 'custom-contact.css' | asset_url | stylesheet_tag }}

  <div
    class="contact-section  "
    style="
      padding-top: {{ section.settings.padding_top }}px;
      padding-bottom: {{ section.settings.padding_bottom }}px;
      padding-left: {{ section.settings.padding_left }}px;
      padding-right: {{ section.settings.padding_right }}px;
    "
  >
    <div class="custom-contact-form">
      {%- if section.settings.heading != blank -%}
        <h2 class="title title-wrapper--no-top-margin inline-richtext {{ section.settings.heading_size }}">
          {{ section.settings.heading }}
        </h2>
      {%- endif -%}
      {%- form 'contact', id: 'ContactForm', class: 'isolate' -%}
        <div class="form-grid">
          <!-- First Name -->
          <div class="field">
            <input type="text" id="ContactFormFirstName" name="contact[first_name]" placeholder="First Name" required>
          </div>

          <!-- Last Name -->
          <div class="field">
            <input type="text" id="ContactFormLastName" name="contact[last_name]" placeholder="Last Name" required>
          </div>

          <!-- Email -->
          <div class="field">
            <input type="email" id="ContactFormEmail" name="contact[email]" placeholder="Email *" required>
          </div>

          <!-- Phone full width -->
          <div class="field">
            <input
              type="tel"
              id="ContactFormPhone"
              name="contact[phone]"
              placeholder="Phone Number"
              pattern="[0-9]{10}"
              maxlength="10"
              required
            >
          </div>
        </div>

        <!-- Message full width -->
        <div class="form-grid">
          <textarea
            id="ContactFormMessage"
            name="contact[body]"
            rows="6"
            placeholder="Message"
            style="min-height: 150px; grid-column: 1 / 3;"
          ></textarea>
        </div>

        <!-- Submit -->
        <button type="submit" class="btn">Send</button>
      {%- endform -%}
    </div>
  </div>

  {% schema %}
  {
    "name": "Custom Contact Form",
    "tag": "section",
    "class": "section",
    "settings": [
      {
        "type": "inline_richtext",
        "id": "heading",
        "label": "Heading",
        "default": "Contact form"
      },
      {
        "type": "checkbox",
        "id": "show_first_name",
        "label": "Show First Name",
        "default": true
      },
      {
        "type": "checkbox",
        "id": "show_second_name",
        "label": "Show Last Name",
        "default": true
      },
      {
        "type": "color_scheme",
        "id": "color_scheme",
        "label": "Color scheme",
        "default": "scheme-1"
      },
      {
        "type": "range",
        "id": "padding_top",
        "min": 0,
        "max": 100,
        "step": 4,
        "unit": "px",
        "label": "Padding top",
        "default": 36
      },
      {
        "type": "range",
        "id": "padding_bottom",
        "min": 0,
        "max": 100,
        "step": 4,
        "unit": "px",
        "label": "Padding bottom",
        "default": 36
      }
    ],
    "presets": [
      {
        "name": "Custom contact form"
      }
    ]
  }
  {% endschema %}
{% endcomment %}
{% comment %}
  <div class="contact page-width page-width--narrow section-{{ section.id }}-padding">
    {%- if section.settings.heading != blank -%}
      <h2 class="title title-wrapper--no-top-margin inline-richtext {{ section.settings.heading_size }}">
        {{ section.settings.heading }}
      </h2>
    {%- endif -%}

    {%- form 'contact', id: 'ContactForm', class: 'isolate' -%}
      <!-- Success & Error Messages -->
      {%- if form.posted_successfully? -%}
        <h2 class="form-status form-status-list form__message">
          {{ 'templates.contact.form.post_success' | t }}
        </h2>
      {%- endif -%}

      <div class="contact__fields">
        <!-- First Name -->
        {% if section.settings.show_first_name %}
          <div class="field">
            <input
              type="text"
              id="ContactFormFirstName"
              class="field__input"
              name="contact[first_name]"
              placeholder="Enter your first name"
              required
            >
            <label class="field__label" for="ContactFormFirstName">First Name</label>
          </div>
        {% endif %}

        <!-- Second Name -->
        {% if section.settings.show_second_name %}
          <div class="field">
            <input
              type="text"
              id="ContactFormSecondName"
              class="field__input"
              name="contact[second_name]"
              placeholder="Enter your last name"
            >
            <label class="field__label" for="ContactFormSecondName">Last Name</label>
          </div>
        {% endif %}
      </div>
      <div class="contact__fields">
        <!-- Email -->
        <div class="field">
          <input
            type="email"
            id="ContactForm-email"
            class="field__input"
            name="contact[email]"
            required
            placeholder="Enter your email"
          >
          <label class="field__label" for="ContactForm-email">Email</label>
        </div>

        <!-- Phone Number -->
        <div class="field">
          <input
            type="tel"
            id="ContactFormPhone"
            class="field__input"
            name="contact[phone]"
            required
            pattern="[0-9]{10}"
            maxlength="10"
            placeholder="Enter 10-digit number"
          >
          <label class="field__label" for="ContactFormPhone">Phone Number</label>
        </div>
      </div>

      <!-- Message -->
      <div class="field">
        <textarea
          id="ContactForm-body"
          class="text-area field__input"
          name="contact[body]"
          rows="6"
          placeholder="Enter your message"
          required
        ></textarea>
        <label class="field__label" for="ContactForm-body">Message</label>
      </div>

      <div class="contact__button">
        <button type="submit" class="button">{{ 'templates.contact.form.send' | t }}</button>
      </div>
    {%- endform -%}
  </div>
{% endcomment %}
{% comment %}
  <div class="custom-contact-form">
    {%- form 'contact', id: 'ContactForm', class: 'isolate' -%}
      <div class="form-grid">
        <!-- First Name -->
        <div class="field">
          <input type="text" id="ContactFormFirstName" name="contact[first_name]" placeholder="First Name" required>
        </div>

        <!-- Email -->
        <div class="field">
          <input type="email" id="ContactFormEmail" name="contact[email]" placeholder="Email *" required>
        </div>
      </div>

      <!-- Phone full width -->
      <div class="field">
        <input
          type="tel"
          id="ContactFormPhone"
          name="contact[phone]"
          placeholder="Phone Number"
          pattern="[0-9]{10}"
          maxlength="10"
          required
        >
      </div>

      <!-- Message full width -->
      <div class="field">
        <textarea id="ContactFormMessage" name="contact[body]" rows="6" placeholder="Message"></textarea>
      </div>

      <!-- Submit -->
      <button type="submit" class="btn">Send</button>
    {%- endform -%}
  </div>
{% endcomment %}
{% comment %}
  <div
    class="contact-section {{ section.settings.color_scheme }}"
    style="
      padding-top: {{ section.settings.padding_top }}px;
      padding-bottom: {{ section.settings.padding_bottom }}px;
      padding-left: {{ section.settings.padding_left }}px;
      padding-right: {{ section.settings.padding_right }}px;
    "
  >
    <div class="custom-contact-form">
      {%- if section.settings.heading != blank -%}
        <h2 class="title title-wrapper--no-top-margin inline-richtext {{ section.settings.heading_size }}">
          {{ section.settings.heading }}
        </h2>
      {%- endif -%}
      {%- form 'contact', id: 'ContactForm', class: 'isolate' -%}
        <div class="form-grid">
          <!-- First Name -->
          <div class="field">
            <input type="text" id="ContactFormFirstName" name="contact[first_name]" placeholder="First Name" required>
          </div>

          <!-- Last Name -->
          <div class="field">
            <input type="text" id="ContactFormLastName" name="contact[last_name]" placeholder="Last Name" required>
          </div>

          <!-- Email -->
          <div class="field">
            <input type="email" id="ContactFormEmail" name="contact[email]" placeholder="Email *" required>
          </div>

          <!-- Phone full width -->
          <div class="field">
            <input
              type="tel"
              id="ContactFormPhone"
              name="contact[phone]"
              placeholder="Phone Number"
              pattern="[0-9]{10}"
              maxlength="10"
              required
            >
          </div>
        </div>

        <!-- Message full width -->
        <div class="form-grid">
          <textarea
            id="ContactFormMessage"
            name="contact[body]"
            rows="6"
            placeholder="Message"
            style="min-height: 150px;grid-column: 1 / 3;"
          ></textarea>
        </div>

        <!-- Submit -->
        <button type="submit" class="btn">Send</button>
      {%- endform -%}
    </div>
  </div>

  {% schema %}
  {
    "name": "Custom Contact Form",
    "tag": "section",
    "class": "section",
    "settings": [
      {
        "type": "inline_richtext",
        "id": "heading",
        "label": "Heading",
        "default": "Contact form"
      },
      {
        "type": "checkbox",
        "id": "show_first_name",
        "label": "Show First Name",
        "default": true
      },
      {
        "type": "checkbox",
        "id": "show_second_name",
        "label": "Show Last Name",
        "default": true
      },
      {
        "type": "color_scheme",
        "id": "color_scheme",
        "label": "Color scheme",
        "default": "scheme-1"
      },
      {
        "type": "range",
        "id": "padding_top",
        "min": 0,
        "max": 100,
        "step": 4,
        "unit": "px",
        "label": "Padding top",
        "default": 36
      },
      {
        "type": "range",
        "id": "padding_bottom",
        "min": 0,
        "max": 100,
        "step": 4,
        "unit": "px",
        "label": "Padding bottom",
        "default": 36
      }
    ],
    "presets": [
      {
        "name": "Custom contact form"
      }
    ]
  }
  {% endschema %}
{% endcomment %}

{% comment %}
  {{ 'custom-contact.css' | asset_url | stylesheet_tag }}

  <div class="contact page-width page-width--narrow section-{{ section.id }}-padding">
    {%- if section.settings.heading != blank -%}
      <h2 class="title title-wrapper--no-top-margin inline-richtext {{ section.settings.heading_size }}">
        {{ section.settings.heading }}
      </h2>
    {%- endif -%}

    {%- form 'contact', id: 'ContactForm', class: 'isolate' -%}
      <!-- Success & Error Messages (same as your code) -->
      {%- if form.posted_successfully? -%}
        <h2 class="form-status form-status-list form__message">{{ 'templates.contact.form.post_success' | t }}</h2>
      {%- endif -%}

      <div class="contact__fields">
        <!-- First Name -->
        {% if section.settings.show_first_name %}
          <div class="field">
            <input
              type="text"
              id="ContactFormFirstName"
              class="field__input"
              name="contact[first_name]"
              placeholder="{{ section.settings.first_name_label }}"
            >
            <label class="field__label" for="ContactFormFirstName">{{ section.settings.first_name_label }}</label>
          </div>
        {% endif %}

        <!-- Second Name -->
        {% if section.settings.show_second_name %}
          <div class="field">
            <input
              type="text"
              id="ContactFormSecondName"
              class="field__input"
              name="contact[second_name]"
              placeholder="{{ section.settings.second_name_label }}"
            >
            <label class="field__label" for="ContactFormSecondName">{{ section.settings.second_name_label }}</label>
          </div>
        {% endif %}

        <!-- Email -->
        <div class="field">
          <input
            type="email"
            id="ContactForm-email"
            class="field__input"
            name="contact[email]"
            required
            placeholder="{{ 'templates.contact.form.email' | t }}"
          >
          <label class="field__label" for="ContactForm-email">{{ 'templates.contact.form.email' | t }}</label>
        </div>
      </div>
      <!-- Phone Number -->
      <div class="field">
        <label for="ContactPhone">Phone Number</label>
        <input
          type="tel"
          id="ContactPhone"
          name="contact[phone]"
          pattern="[0-9]{10}"
          maxlength="10"
          required
          placeholder="Enter 10-digit number"
        >
      </div>

      <!-- Message -->
      <div class="field">
        <textarea
          id="ContactForm-body"
          class="text-area field__input"
          name="contact[body]"
          rows="6"
          placeholder="{{ 'templates.contact.form.comment' | t }}"
        ></textarea>
        <label class="field__label" for="ContactForm-body">{{ 'templates.contact.form.comment' | t }}</label>
      </div>

      <div class="contact__button">
        <button type="submit" class="button">{{ 'templates.contact.form.send' | t }}</button>
      </div>
    {%- endform -%}
  </div>
  {% schema %}
  {
    "name": "Custom Contact Form",
    "tag": "section",
    "class": "section",
    "settings": [
      {
        "type": "inline_richtext",
        "id": "heading",
        "label": "Heading",
        "default": "Contact form"
      },
      {
        "type": "checkbox",
        "id": "show_first_name",
        "label": "Show First Name",
        "default": true
      },
      {
        "type": "text",
        "id": "first_name_label",
        "label": "First Name Label",
        "default": "First Name"
      },
      {
        "type": "checkbox",
        "id": "show_second_name",
        "label": "Show Second Name",
        "default": true
      },
      {
        "type": "text",
        "id": "second_name_label",
        "label": "Second Name Label",
        "default": "Second Name"
      },
      {
        "type": "text",
        "id": "ContactPhone",
        "label": "Phone Number Label",
        "default": "Phone Number"
      },
      {
        "type": "color_scheme",
        "id": "color_scheme",
        "label": "Color scheme",
        "default": "scheme-1"
      },
      {
        "type": "range",
        "id": "padding_top",
        "min": 0,
        "max": 100,
        "step": 4,
        "unit": "px",
        "label": "Padding top",
        "default": 36
      },
      {
        "type": "range",
        "id": "padding_bottom",
        "min": 0,
        "max": 100,
        "step": 4,
        "unit": "px",
        "label": "Padding bottom",
        "default": 36
      }
    ],
    "presets": [
      {
        "name": "Custom contact form"
      }
    ]
  }
  {% endschema %}
{% endcomment %}

{% comment %}
  {% schema %}
  {
    "name": "Contact form",
    "settings": [
      {
        "type": "text",
        "id": "form_heading",
        "label": "Heading",
        "default": "Contact form"
      },
      {
        "type": "text",
        "id": "name_label",
        "label": "Name Field Label",
        "default": "Your Name"
      },
      {
        "type": "text",
        "id": "email_label",
        "label": "Email Field Label",
        "default": "Your Email"
      },
      {
        "type": "text",
        "id": "message_label",
        "label": "Message Field Label",
        "default": "Your Message"
      },
      {
        "type": "text",
        "id": "button_label",
        "label": "Submit Button Text",
        "default": "Send"
      }
    ],
    "presets": [
      {
        "name": "Contact form"
      }
    ]
  }
  {% endschema %}
  <div class="custom-contact-form">
    <h2>{{ section.settings.form_heading }}</h2>
    <form method="post" action="/contact#contact_form">
      <div>
        <label for="ContactFormName">{{ section.settings.name_label }}</label>
        <input type="text" name="contact[name]" id="ContactFormName" required>
      </div>
      <div>
        <label for="ContactFormEmail">{{ section.settings.email_label }}</label>
        <input type="email" name="contact[email]" id="ContactFormEmail" required>
      </div>
      <div>
        <label for="ContactFormMessage">{{ section.settings.message_label }}</label>
        <textarea name="contact[body]" id="ContactFormMessage"></textarea>
      </div>
      <button type="submit">{{ section.settings.button_label }}</button>
    </form>
  </div>
{% endcomment %}

{% comment %}
  {% assign section = section.settings %}
  <section class="custom-contact">
    <div class="custom-contact-wrapper">
      <h2 class="custom-contact-title">{{ section.settings.title }}</h2>

      <form
        method="post"
        action="/contact#contact_form"
        id="contact_form"
        accept-charset="UTF-8"
        class="custom-contact-form"
      >
        <input type="hidden" name="form_type" value="contact">
        <input type="hidden" name="utf8" value="✓">

        <!-- Default fields -->
        <div class="field">
          <label for="ContactFormName">Name</label>
          <input type="text" id="ContactFormName" name="contact[name]" required>
        </div>

        <div class="field">
          <label for="ContactFormEmail">Email</label>
          <input type="email" id="ContactFormEmail" name="contact[email]" required>
        </div>

        <!-- Dynamic merchant-added fields -->
        {% for block in section.blocks %}
          <div class="field">
            <label for="field-{{ block.id }}">{{ block.settings.label }}</label>

            {% if block.settings.type == 'text' %}
              <input type="text" id="field-{{ block.id }}" name="contact[{{ block.settings.label | handleize }}]">
            {% elsif block.settings.type == 'textarea' %}
              <textarea id="field-{{ block.id }}" name="contact[{{ block.settings.label | handleize }}]"></textarea>
            {% elsif block.settings.type == 'select' %}
              <select id="field-{{ block.id }}" name="contact[{{ block.settings.label | handleize }}]">
                {% for option in block.settings.options | split: ',' %}
                  <option value="{{ option | strip }}">{{ option | strip }}</option>
                {% endfor %}
              </select>
            {% endif %}
          </div>
        {% endfor %}

        <button type="submit" class="contact-submit">{{ section.settings.button_text }}</button>
      </form>
    </div>
  </section>

  {% schema %}
  {
    "name": "Custom Contact Form",
    "settings": [
      {
        "type": "text",
        "id": "title",
        "label": "Form Title",
        "default": "Get in Touch"
      },
      {
        "type": "text",
        "id": "button_text",
        "label": "Button text",
        "default": "Send Message"
      }
    ],
    "blocks": [
      {
        "type": "field",
        "name": "Custom Field",
        "settings": [
          {
            "type": "text",
            "id": "label",
            "label": "Field Label",
            "default": "Phone"
          },
          {
            "type": "select",
            "id": "type",
            "label": "Field Type",
            "options": [
              { "value": "text", "label": "Text" },
              { "value": "textarea", "label": "Textarea" },
              { "value": "select", "label": "Dropdown" }
            ],
            "default": "text"
          },
          {
            "type": "text",
            "id": "options",
            "label": "Dropdown Options (comma separated)",
            "default": "Option 1, Option 2",
            "info": "Only used if 'Dropdown' is selected"
          }
        ]
      }
    ],
    "max_blocks": 10,
    "presets": [
      {
        "name": "Custom Contact Form"
      }
    ]
  }
  {% endschema %}
{% endcomment %}

{% comment %}
  <div>
    <h2 class="section-title">{{ section.settings.heading }}</h2>

    {% form 'contact' %}
      {%- if form.posted_successfully? -%}
        <p>posted_successfully</p>
      {%- elsif form.errors -%}
        {{ form.errors | default_errors }}
      {%- endif -%}

      {% for block in section.blocks %}
        <div class="contact-field contact-field--{{ block.type }}">
          {% case block.type %}
            {% when 'text' %}
              <label for="{{ block.id }}">{{ block.settings.label }}</label>
              <input
                type="text"
                name="contact[{{ block.settings.name | handle }}]"
                id="{{ block.id }}"
                placeholder="{{ block.settings.placeholder }}"
              >

            {% when 'email' %}
              <label for="{{ block.id }}">{{ block.settings.label }}</label>
              <input
                type="email"
                name="contact[{{ block.settings.name | handle }}]"
                id="{{ block.id }}"
                placeholder="{{ block.settings.placeholder }}"
              >

            {% when 'tel' %}
              <label for="{{ block.id }}">{{ block.settings.label }}</label>
              <input
                type="tel"
                name="contact[{{ block.settings.name | handle }}]"
                id="{{ block.id }}"
                placeholder="{{ block.settings.placeholder }}"
              >

            {% when 'textarea' %}
              <label for="{{ block.id }}">{{ block.settings.label }}</label>
              <textarea
                name="contact[{{ block.settings.name | handle }}]"
                id="{{ block.id }}"
                placeholder="{{ block.settings.placeholder }}"
              ></textarea>

            {% when 'checkbox' %}
              <label>
                <input
                  type="checkbox"
                  name="contact[{{ block.settings.name | handle }}]"
                  id="{{ block.id }}"
                >
                {{ block.settings.label }}
              </label>
          {% endcase %}
        </div>
      {% endfor %}

      <div>
        <button type="submit" class="btn">Send</button>
      </div>
    {% endform %}
  </div>

  {% schema %}
  {
    "name": "Custom contact",
    "tag": "section",
    "class": "section",
    "settings": [
      {
        "type": "text",
        "id": "heading",
        "label": "Heading",
        "default": "Contact Us"
      }
    ],
    "blocks": [
      {
        "type": "text",
        "name": "Text input",
        "settings": [
          { "type": "text", "id": "label", "label": "Label", "default": "Your name" },
          { "type": "text", "id": "name", "label": "Input name (no spaces)", "default": "name" },
          { "type": "text", "id": "placeholder", "label": "Placeholder", "default": "Enter text" }
        ]
      },
      {
        "type": "email",
        "name": "Email input",
        "settings": [
          { "type": "text", "id": "label", "label": "Label", "default": "Email" },
          { "type": "text", "id": "name", "label": "Input name (no spaces)", "default": "email" },
          { "type": "text", "id": "placeholder", "label": "Placeholder", "default": "Enter email" }
        ]
      },
      {
        "type": "tel",
        "name": "Phone input",
        "settings": [
          { "type": "text", "id": "label", "label": "Label", "default": "Phone" },
          { "type": "text", "id": "name", "label": "Input name (no spaces)", "default": "phone" },
          { "type": "text", "id": "placeholder", "label": "Placeholder", "default": "Enter phone number" }
        ]
      },
      {
        "type": "textarea",
        "name": "Textarea",
        "settings": [
          { "type": "text", "id": "label", "label": "Label", "default": "Message" },
          { "type": "text", "id": "name", "label": "Input name (no spaces)", "default": "message" },
          { "type": "text", "id": "placeholder", "label": "Placeholder", "default": "Enter your message" }
        ]
      },
      {
        "type": "checkbox",
        "name": "Checkbox",
        "settings": [
          { "type": "text", "id": "label", "label": "Label", "default": "I agree" },
          { "type": "text", "id": "name", "label": "Input name (no spaces)", "default": "agree" }
        ]
      }
    ],
    "presets": [
      {
        "name": "Custom contact"
      }
    ]
  }
  {% endschema %}
{% endcomment %}
{% comment %}
  <h2 class="section-title">Contact Us</h2>

  {% form 'contact' %}
    {%- if form.posted_successfully? -%}
      <p>posted_successfully</p>
    {%- elsif form.errors -%}
      {{ form.errors | default_errors }}
    {%- endif -%}

    <div className="first-name">
      <label htmlFor="first-name">First name</label>
      <input type="text" name="contact[first_name]" placeholder="Enter your first name" id="first-name">
    </div>

    <div className="last-name">
      <label htmlFor="last-name">Last name</label>
      <input type="text" name="contact[last_name]" placeholder="Enter your last name" id="last-name">
    </div>

    <div className="phone">
      <label htmlFor="phone">Phone</label>
      <input type="tel" name="contact[phone]" placeholder="Enter your phone number" id="phone">
    </div>

    <div className="email">
      <label htmlFor="email">Email</label>
      <input type="email" name="contact[email]" placeholder="Enter your email" id="email">
    </div>
    <div className="order-number">
      <label htmlFor="order-number">Order number</label>
      <input type="text" name="contact[order_number]" id="order-number">
    </div>
    <div className="urgent-issue">
      <label htmlFor="urgent-issue">This is Urgent issue</label>
      <input type="checkbox" name="contact[urgent_issue]" id="urgent-issue">
    </div>

    <div className="message">
      <label htmlFor="message">Message</label>
      <textarea name="contact[message]" placeholder="Enter your message" id="message"></textarea>
    </div>
    <div>
      <button type="submit" class="btn">Send</button>
    </div>
  {% endform %}

  {% schema %}
  {
    "name": "custom contact",
    "tag": "section",
    "class": "section",
    "settings": [],
    "presets": [
      {
        "name": "custom contact"
      }
    ]
  }
  {% endschema %}
{% endcomment %}
